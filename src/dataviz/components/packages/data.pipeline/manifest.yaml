schema: dataviz/component-package/v1
package: data.pipeline
version: 1.0.0
category: data
purpose: Named Output and transform contracts between data acquisition and presentation.
dependencies: []
components:
  - {id: output.named, purpose: Stable typed addressable output from a Source or Transform, status: stable}
  - {id: dataset-transform.server-python, purpose: Query-stage Python transformation producing Base Outputs, status: stable}
  - {id: interactive-transform.browser-js, purpose: Interaction-stage JavaScript transformation producing Derived Outputs, status: stable}
  - {id: interactive-transform.browser-python, purpose: Interaction-stage Pyodide transformation producing portable Derived Outputs, status: stable}
  - {id: interactive-transform.server-python, purpose: Interaction-stage server Python computation against one immutable Query Run, status: stable}
runtime: {controller: controller.js, adapter: adapter.js, style: style.css}
