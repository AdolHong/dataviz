# 0.25.9 本地打包记录

用户要求从 0.25.8 增加一个 patch 并打包。包含尚未发布的 Run 取消隔离、完成状态重查、
Live 资源清理，以及可靠性测试和文档修正；DSL 不变，配套 Skill 继续随包提供。

## 复用的验证与风险

运行时改动后的非浏览器全套 826 passed；三浏览器核心各 16 passed。
完整 Chromium 163 passed；WebKit 首次 153 passed、1 failed，级联等待断言修正后
三端定向通过。Firefox 首次在导航用例超时；续跑 103 passed、2 failed，两项级联排序
断言修正后定向通过。导航独立及与前置用例组合都通过，但原始停滞仍未解释。

具体日志、覆盖范围与安装矩阵见 [可靠性证据](reliability-95-evidence.md) 和
[异步审查](async-recovery-resource-review.md)。这些是已有工作树证据，不声称在
0.25.9 重新执行完整矩阵，不声称远端 CI 通过。此轮只做本地构建，不上传包仓库。

Firefox 停滞属于已披露风险；此次打包不代表问题修复或达到 9.5。
