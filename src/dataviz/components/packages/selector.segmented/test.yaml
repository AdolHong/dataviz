schema: dataviz/component-test/v1
tests:
  - id: selector.segmented.single-state
    component: selector.segmented
    kind: browser
    assertions: [only one value is selected, All clears include values, canonical native state emits change]
  - id: selector.segmented.keyboard
    component: selector.segmented
    kind: accessibility
    assertions: [every option is keyboard reachable, radio roles expose checked state]
