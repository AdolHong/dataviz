[project]
name = "workspace-dataviz"
version = "0.2.0"
description = "Workspace-first data dashboards for humans and AI"
readme = "README.md"
requires-python = ">=3.11,<3.15"
dependencies = [
  "duckdb>=1.3",
  "fastapi>=0.116",
  "jinja2>=3.1",
  "packaging>=24",
  "pandas>=2.2",
  "plotly>=6.1",
  "pymysql>=1.1",
  "pyarrow>=18",
  "pydantic>=2.10",
  "pyyaml>=6.0",
  "sqlalchemy>=2.0",
  "typer>=0.15",
  "uvicorn>=0.34",
]

[project.optional-dependencies]
excel = ["openpyxl>=3.1", "xlrd>=2.0"]
dev = ["httpx>=0.28", "playwright>=1.55", "pytest>=8.3", "ruff>=0.12"]

[project.scripts]
dataviz = "dataviz.cli:app"

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
dataviz = ["server/static/*", "server/templates/*", "gallery/**/*", "components/packages/**/*"]

[tool.setuptools.exclude-package-data]
dataviz = [
  "gallery/.dataviz/*",
  "gallery/.dataviz/**/*",
  "gallery/**/adapters.local.yaml",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
pythonpath = ["src"]
markers = ["e2e: real-browser runtime tests"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E9", "F", "B"]
ignore = ["B008"] # Typer declares CLI metadata through function defaults by design.
