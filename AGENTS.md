# Dataviz 开发代理规则

本文件适用于本仓库的开发、测试和发布工作。面向看板作者的使用方法维护在
`dataviz-skill.md`；不要把开发者测试流程或人工操作页面的说明塞进该 Skill。

## 测试范围：按影响选择，不默认全量

- 默认执行增量验证。先说明改动影响、准备运行的测试及选择理由，再执行。
- 用户明确要求全量测试时，按要求执行；单纯“升版”“打包”“发布新版本”不自动等于全量测试。
- 纯文档修改：检查文档一致性、引用及受影响的示例／契约，不启动浏览器全套。
- 单个组件或局部功能修改：运行相关单元／契约测试，以及必要的组件或定向浏览器测试。
- 公共运行时、跨页面状态或共享交互修改：运行相关回归，并扩大到三浏览器核心场景。
- 广泛架构调整、关键浏览器依赖升级，或有明确全面验收要求的发布：运行完整浏览器矩阵。
- 浏览器分层入口为 `scripts/test_browsers.py --suite components|core|extended|full`；核心场景位于 `tests/e2e/core`。失败后用 `--browsers` 和 `-- -k ...` 缩小范围，不重新跑整层。

测试入口、资源缓存和证据说明见 [tests/e2e/README.md](tests/e2e/README.md)。
覆盖合并的理由与保留边界见 [docs/browser-test-pruning.md](docs/browser-test-pruning.md)。
不要重新引入被合并的宽度／页面全排列或固定 Chromium CLI 的三分组重复；新增排列须说明独立风险。
`scripts/test_browsers.py` 不加筛选参数会运行全套；定向验证须显式选择引擎和用例。
确认实际选中了预期用例，不能把零用例、跳过或收集失败算作通过。

## 失败后只扩大到必要范围

1. 保留首次失败日志、命令、代码版本／工作树状态和必要的 trace；不要覆盖原始证据。
2. 优先单独复现失败用例，区分产品缺陷、测试前提错误和环境故障。
3. 无代码改动的重试通过只能说明可能偶发，不能抹掉首次失败或宣称问题已修复。
   没有新的诊断假设，不要反复运行同一命令直到变绿。
4. 修复后先运行失败用例及受影响的相关测试。若修改公共运行时，再扩大到三浏览器核心场景。
5. 只有影响范围足够广、现有证据无法覆盖风险，或用户明确要求时，才重新运行完整矩阵；扩大前说明原因。

禁止仅因为一个用例失败，就机械地重跑全部测试或全部浏览器。
若仅修正定位、等待条件或测试数据，产品代码未变，通常只重跑修改后的用例及必要关联场景；
不要求未受影响的浏览器重复跑完整套件。
共享测试 fixture 或测试工具的改动也要评估影响，不能因为“只是测试代码”就一律认定无影响。

## 已有结果的复用与失败处理

- 仅当相关产品代码、依赖、测试前提和环境条件未变化时，才复用已有通过结果。
  记录对应证据位置和适用范围；复用证据不等于宣称在最新版本重新跑过全套。
- 产品修复使相关旧结果失效，但不自动使所有无关场景的结果失效。
- 产品缺陷应修复并保留确定性回归；尽量将回归下沉到快速、独立的小测试。
- 环境故障先修正环境，再跑受影响用例。资源缓存必须校验完整性，不能关闭校验换取绿灯。
- 不得在仍有未解释的相关失败时声称验证完成。若无法定位，应明确风险和未验证范围，请求发布方向。

## 测试的删除、拆分与隔离

- 可以删除过时或重复的测试，但先说明原本保障的用户行为，以及为何过时或由什么覆盖替代。
- 优先拆分同时验证多个独立行为的长链路测试；焦点、尺寸等局部行为优先使用组件级场景。
- 不得为通过而删除真实缺陷回归、吞掉异常、放宽关键断言，或用任意延时／增加重试次数掩盖竞态。
- 暂时隔离不稳定用例时，保留失败记录、原因和恢复条件；不能静默 skip 或自动降低现有 CI 发布门禁。

## 执行成本与发布汇报

- 长测试交给运行器管理进度、日志和失败摘要；避免不断加载完整日志或让模型重复分析无变化的输出。
- 不因单纯升版、打包再次触发完整矩阵。检查版本一致性、构建产物和必要安装行为即可；有相关代码变化时按上述范围追加测试。
- 现有 CI 发布门禁仍须遵守。本规则不授权绕过门禁，也不自动修改 CI；若门禁需要减重，单独调整并验证配置。
- 完成时明确报告：本次执行范围与结果、复用的证据、未覆盖范围及理由，以及仍未解决的问题。
- 定向通过不得表述为“完整套件通过”；本地通过不得表述为“远端 CI 已通过”。
- 只有实际构建并核验后才能声称“已打包”；不把源码测试当作安装包验证。
