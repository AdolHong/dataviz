schema: dataviz/component-story/v1
stories:
  - id: selector.select.scale-10
    component: selector.select
    title: Select · 10 options
    summary: A compact real workload keeps the full option list visible without forced search.
    gallery: {dashboard: component-gallery, target: "#story-selector-scale-10", anchor: selector-select-10}
  - id: selector.select.scale-100
    component: selector.select
    title: Select · 100 options
    summary: Search activates against one hundred real native options without changing the public template.
    gallery: {dashboard: component-gallery, target: "#story-selector-scale-100", anchor: selector-select-100}
  - id: selector.select.scale-1000
    component: selector.select
    title: Select · 1,000 options
    summary: Search covers one thousand real native options while the enhanced row DOM stays bounded.
    gallery: {dashboard: component-gallery, target: "#story-selector-scale-1000", anchor: selector-select-1000}
  - id: selector.select.grouped
    component: selector.select
    title: Grouped options and compact selected tags
    summary: Choice metadata remains searchable while the closed trigger keeps a bounded height.
    gallery: {dashboard: component-gallery, target: "[data-selector-template='select']", anchor: selector-select-grouped}
