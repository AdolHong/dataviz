schema: dataviz/component-story/v1
stories:
  - id: selector.checkbox-group.small-multi
    component: selector.checkbox-group
    title: Small visible multi-selection
    summary: Select all keeps every checkbox explicit; the action becomes Invert so users can remove a few or return to an unconstrained empty state.
    gallery: {dashboard: component-gallery, target: "[data-selector-template='checkbox-group']", anchor: selector-checkbox-group}
  - id: selector.checkbox-group.tags
    component: selector.checkbox-group
    title: Checkable tag variant
    summary: A denser visual variant preserves explicit full, subset and empty canonical states.
    gallery: {dashboard: component-gallery, target: "[data-selector-template='checkbox-group']", anchor: selector-checkbox-tags}
