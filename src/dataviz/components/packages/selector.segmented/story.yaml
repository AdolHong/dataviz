schema: dataviz/component-story/v1
stories:
  - id: selector.segmented.small-single
    component: selector.segmented
    title: Visible small single selection
    summary: Up to four peer options remain directly comparable, with All representing an empty include-selection.
    gallery: {dashboard: component-gallery, target: "[data-selector-template='segmented']", anchor: selector-segmented}
  - id: selector.segmented.radio-variant
    component: selector.segmented
    title: Radio presentation variant
    summary: The same canonical state can use a wrapping radio language when space is narrow.
    gallery: {dashboard: component-gallery, target: "[data-selector-template='segmented']", anchor: selector-segmented-radio}
