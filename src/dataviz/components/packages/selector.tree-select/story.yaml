schema: dataviz/component-story/v1
stories:
  - id: selector.tree-select.location
    component: selector.tree-select
    title: Searchable location tree
    summary: Expand hierarchy branches or search complete paths in one narrow overlay.
    gallery: {dashboard: component-gallery, target: "[data-selector-template='tree-select']", anchor: selector-tree-select}
