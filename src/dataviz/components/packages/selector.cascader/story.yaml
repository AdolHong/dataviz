schema: dataviz/component-story/v1
stories:
  - id: selector.cascader.location
    component: selector.cascader
    title: Province / city / district
    summary: Browse branches independently and select leaves across more than one city.
    gallery: {dashboard: component-gallery, target: "[data-selector-template='cascader']", anchor: selector-cascader}
