schema: dataviz/dashboard/v2
kind: dashboard
id: worker-runtime
title: Browser JS Interactive Worker

compute_parameters:
  - id: delay_ms
    label: Worker delay
    type: number
    default: 5

sources:
  - id: raw
    type: file
    path: data/rows.csv
    format: csv
    outputs: {main: {kind: table}}

interactive_transforms:
  - transforms/scaled.yaml

views:
  - id: scaled-table
    title: Scaled rows
    input: interactive:scaled/main
    template: table
    columns: [name, value]

sections:
  - id: results
    title: Results
    views: [scaled-table]
