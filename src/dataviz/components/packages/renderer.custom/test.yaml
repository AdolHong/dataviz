schema: dataviz/component-test/v1
tests:
  - {id: renderer.custom.lifecycle, component: renderer.custom, kind: contract, assertions: [validate, mount, update, dispose, async-await, view-scoped-error, plotly-chart-service, tanstack-table-service, raw-table-core]}
