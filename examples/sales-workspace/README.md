# Sales Workspace

这个 workspace 用于验证：

- File、SQL 两类 Source 与 Python Dataset Transform
- 浏览器端 Plotly、ECharts 和表格 View
- 节点级执行状态
- 可选的 Dashboard 自定义 HTML/CSS Canvas
- CLI HTML 报告

运行：

```bash
dataviz validate .
dataviz serve .
dataviz report . sales --output dist/sales.html
```
