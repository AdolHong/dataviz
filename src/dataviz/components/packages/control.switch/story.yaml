schema: dataviz/component-story/v1
stories:
  - id: control.switch.immediate
    component: control.switch
    title: Immediate boolean toggle
    summary: Switch emits the canonical boolean immediately; query execution policy still belongs to the surrounding workflow.
    gallery: {dashboard: component-gallery, target: "[data-control-component='switch']", anchor: control-switch}
